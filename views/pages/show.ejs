<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="show.css" type="text/css">

</head>
<body>
    <div id="container">
        
        <section id="section">
            <h2><%= show.name %> - Edit</h2>
            <form action="/updateOne/<%= show.id %>" method="patch" id="form-input">
                <div class="form-input">
                    <label for="name">Editeaza numele:</label>
                    <input type="text" name="name" id="name" value=<%= show.name %>>
                </div>
                <div class="form-input">
                    <label for="description">Editeaza descrierea:</label>
                    <textarea name="description" id="description" cols="30" rows="3"><%= show.description %></textarea>
                </div>
                <div class="form-input">
                    <label for="review">Editeaza review-ul</label>
                    <textarea name="review" id="review" cols="30" rows="3"><%= show.review %></textarea>
                </div>
                <div class="form-input">
                    <label for="completed">Completat:</label>
                    <input type="checkbox" name="completed" id="completed" 
                        <% if (show.completed === true) { %>
                            checked
                        <% } %>
                    >
                </div>
                <input type="submit" id="submit" value="Submit">
            </form>
            <a href="/"><button id="back">Inapoi pe pagina principala</button></a>
        </section>
    
        <footer>
            <%- include('../partials/footer') %>
        </footer>
    </div>
</body>
</html>